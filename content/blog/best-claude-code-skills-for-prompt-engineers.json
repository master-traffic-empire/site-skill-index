{
  "slug": "best-claude-code-skills-for-prompt-engineers",
  "title": "The 8 Best Claude Code Skills for Prompt Engineers (2026)",
  "description": "A curated list of the most useful Claude Code skills for prompt engineers in 2026 — a cross-model eval harness with an LLM judge, schema-driven structured-output extraction, a way to codify a working flow into a reusable skill, a persistent prompt-pattern library, context save and restore for long iteration sessions, an Obsidian-backed prompt vault, and clean document parsing for grounded context. All from verified plugins with real star counts and real commit history on GitHub.",
  "content": "<p>Prompt engineering is an experimental discipline that pretends to be a writing one. The output is a prompt, but the work is a loop: draft a version, run it against real inputs, score the results, compare it to the last version and to other models, keep what wins, and write down why. The hard part is not wording a prompt cleverly; it is doing that loop with enough rigor that &quot;v4 is better&quot; means something measurable instead of a vibe. The eight skills below map onto that loop — eval and judging, structured output, versioning and reuse, a pattern library, context carry-over across long sessions, and clean grounding data. Each is a real, verified Claude Code skill from a plugin with public commit history and a real star count on GitHub, and Skill Index has the exact install command on each detail page.</p><h2>1. benchmark-models &mdash; by gstack</h2><p>From the <code>gstack</code> plugin (104,138 stars, MIT, verified &mdash; the largest plugin in the index). It runs the same prompt through Claude, GPT (via the Codex CLI), and Gemini side-by-side and compares latency, tokens, cost, and optionally quality via an LLM judge. For a prompt engineer this is the single most useful thing in the index, because it answers the question that otherwise gets answered by guesswork: which model is actually best for this prompt, with data instead of vibes. The LLM-judge option is the part that matters most &mdash; it turns &quot;this response feels better&quot; into a scored, repeatable comparison you can run again after you change one line of the prompt. It is deliberately separate from <code>gstack</code>'s page-performance <code>benchmark</code> skill, so the name does not collide with web-perf work.</p><p><strong>When to use:</strong> every time you have two candidate prompts or two candidate models and you are about to pick one on instinct. Run it, read the judge score and the token-and-cost columns together, and pick the version that wins on quality at an acceptable cost. Pair it with the <a href=\"/blog/best-claude-code-skills-for-ml-engineers\">ml-engineers skill stack</a> for the model-evaluation context around it.</p><p><a href=\"/skills/gstack/benchmark-models\">Learn more &rarr;</a></p><h2>2. firecrawl-agent &mdash; by cli</h2><p>From the <code>cli</code> plugin (425 stars, verified, by Firecrawl). An autonomous extraction agent that navigates complex multi-page sites and returns structured JSON against a schema you supply. The prompt-engineering use is structured output the honest way: instead of begging a prompt to &quot;respond only in JSON&quot; and praying it holds, you hand the tool an explicit JSON schema and get conformant data back. That habit &mdash; constrain the shape, then validate against it &mdash; is the same discipline you want in every production prompt that feeds a downstream system. It is also a fast way to build the input side of an eval set: point it at real pages and get clean, schema-shaped records to test prompts against.</p><p><strong>When to use:</strong> any time you need real structured data &mdash; pricing tiers, product listings, directory entries &mdash; as test inputs for a prompt, or any time you are designing a structured-output prompt and want a working schema-constrained pattern to copy. Pair it with the <a href=\"/blog/best-claude-code-skills-for-data-analysts\">data-analysts skill stack</a> when the extracted records are something you then need to trend rather than feed to a model.</p><p><a href=\"/skills/cli/firecrawl-agent\">Learn more &rarr;</a></p><h2>3. skillify &mdash; by gstack</h2><p>Also from <code>gstack</code>. It walks back through the conversation, takes the most recent successful flow, and codifies it into a permanent, tested skill on disk &mdash; script, test, and fixture &mdash; so the next run executes the saved version instead of re-deriving it. For a prompt engineer this is prompt versioning with teeth: the prompt or flow that finally worked stops living in a scrollback you will lose and becomes a named, committed, test-covered artifact. The generated test is the quiet payoff &mdash; it pins the behavior, so when you tweak the prompt later you find out immediately whether you improved it or broke it, which is exactly the regression check most prompt work never has.</p><p><strong>When to use:</strong> the moment a prompt or flow crosses from &quot;experimenting&quot; to &quot;I want this exact thing again,&quot; before the working version scrolls out of reach. Treat the committed skill as the canonical version of that prompt. Pair it with <code>benchmark-models</code> above so the version you codify is the one that actually won the comparison.</p><p><a href=\"/skills/gstack/skillify\">Learn more &rarr;</a></p><h2>4. learn &mdash; by gstack</h2><p>Also from <code>gstack</code>. A persistent store of what the project has learned across sessions, with commands to review, search, prune, and export it, plus a proactive nudge when you ask about a past pattern or wonder &quot;didn't we solve this before?&quot; For a prompt engineer this is the pattern library that actually gets used, because it is searched from inside the same tool doing the work rather than living in a doc nobody opens. The recurring waste in prompt work is rediscovering the same fix &mdash; the phrasing that stops a model from hedging, the system-prompt clause that fixes a refusal, the few-shot ordering that lifts accuracy &mdash; and a searchable learnings store is the cheapest defense against re-solving it from scratch every time.</p><p><strong>When to use:</strong> write a learning every time a prompt change produces a non-obvious improvement, and search it at the start of every new prompt before you reinvent a technique you already proved out. Prune on a cadence so stale tricks do not mislead. For prompts and rubrics that need to live as real, linkable documents, pair it with the <code>obsidian-cli</code> skill below.</p><p><a href=\"/skills/gstack/learn\">Learn more &rarr;</a></p><h2>5. context-save &mdash; by gstack</h2><p>Also from <code>gstack</code>. It captures git state, the decisions made so far, and the remaining work into a saved context that a later session can pick up without losing the thread. Prompt iteration is long: by version ten you are holding a dozen tradeoffs in your head &mdash; why you dropped the chain-of-thought block, which few-shot example caused the regression, what the judge flagged last run. A context window does not survive a session boundary, and re-deriving that state the next morning is pure waste. The skill turns the end of a session into a durable artifact: what worked, what failed, what is still open.</p><p><strong>When to use:</strong> at the end of any prompt-iteration session you will not finish in one sitting, and before you step away from a long eval run. Save before you log off, not after you have already lost the thread. Pair it with <code>context-restore</code> below to resume.</p><p><a href=\"/skills/gstack/context-save\">Learn more &rarr;</a></p><h2>6. context-restore &mdash; by gstack</h2><p>Also from <code>gstack</code>. The other half of the pair: it loads the most recent saved state &mdash; across branches and even across workspace handoffs &mdash; so you resume exactly where the last session ended. For a prompt engineer the value is continuity across a multi-day iteration, where the cost of a cold start is re-reading your own diffs to remember which prompt variant was the current best.</p><p><strong>When to use:</strong> at the start of every session that continues prompt work from a previous day, and any time a teammate hands off an in-progress iteration to you. Run it before you touch the prompt, so you are editing the version that was actually winning. Pair it with <code>context-save</code> above and with <code>learn</code> so the durable wins are searchable even after the session context is gone.</p><p><a href=\"/skills/gstack/context-restore\">Learn more &rarr;</a></p><h2>7. obsidian-cli &mdash; by obsidian-skills</h2><p>From the <code>obsidian-skills</code> plugin (33,445 stars, MIT, verified). It reads, creates, searches, and manages notes in an Obsidian vault straight from the command line. For a prompt engineer this is the durable home for the prompt library &mdash; the place where reusable system prompts, few-shot sets, eval rubrics, and the reasoning behind each version live as real, linkable, searchable documents instead of scattered text files. The advantage over the in-session <code>learn</code> store is permanence and structure: wikilinks let you connect a prompt to its eval results and its judge rubric, so the library is a graph you can navigate rather than a flat log. Build prompts in the tool that runs them, then file the keepers in the vault.</p><p><strong>When to use:</strong> as the system of record for any prompt you will reuse or share &mdash; save the final version, its few-shot examples, and the rubric you judged it against as linked notes. Pair it with <code>learn</code> so the fast in-session capture and the durable vault stay in sync, and with the <a href=\"/blog/best-claude-code-skills-for-technical-writers\">technical-writers skill stack</a> when the prompts you are documenting are part of a product spec.</p><p><a href=\"/skills/obsidian-skills/obsidian-cli\">Learn more &rarr;</a></p><h2>8. firecrawl-parse &mdash; by cli</h2><p>Also from the <code>cli</code> plugin (425 stars, verified, by Firecrawl). It turns a local document &mdash; PDF, DOCX, XLSX, HTML, and more &mdash; into clean markdown on disk, with options for an AI summary or a direct question against the file. The prompt-engineering use is grounding-context preparation: the quality of a RAG or retrieval prompt is capped by the quality of the text you feed it, and a PDF dumped raw into a prompt is full of headers, footers, and layout noise that wastes tokens and confuses the model. This produces the clean markdown you actually want as context, saved to disk so you read it incrementally with <code>grep</code> and <code>head</code> rather than blowing up the window. Better grounding data is the highest-leverage, least-glamorous fix in most retrieval prompts.</p><p><strong>When to use:</strong> any time a prompt needs a local document as grounding context &mdash; parse it first, inspect the markdown, then feed only the relevant sections. Treat clean parsing as a prerequisite for any retrieval prompt rather than an afterthought. Pair it with <code>firecrawl-agent</code> above when the grounding data lives on the web rather than on disk.</p><p><a href=\"/skills/cli/firecrawl-parse\">Learn more &rarr;</a></p><h2>How to install</h2><p>Five of the eight skills live in the <code>gstack</code> plugin (104,138 stars on <code>garrytan/gstack</code>, MIT, verified &mdash; the largest and most active plugin in the index), two in <code>cli</code> (425 stars, verified, by Firecrawl), and one in <code>obsidian-skills</code> (33,445 stars, MIT, verified), so install is a three-marketplace operation &mdash; Skill Index has the exact install command on each skill detail page with a copy button. The highest-ROI sequence for a prompt engineer's iteration loop: make <code>benchmark-models</code> the gate every prompt-or-model choice passes through, so picks are scored by an LLM judge instead of intuition. Use <code>firecrawl-agent</code> to build schema-shaped test inputs and <code>firecrawl-parse</code> to prepare clean grounding context. When a version finally wins, <code>skillify</code> it into a tested, committed artifact, write the why into <code>learn</code>, and file the keeper in your Obsidian vault via <code>obsidian-cli</code>. Bracket every long iteration with <code>context-save</code> at the end and <code>context-restore</code> at the start so a multi-day prompt never cold-starts. Pair the deep iteration sessions with focused blocks via <a href=\"https://focus.thicket.sh\">focus.thicket.sh</a>, and the work stops being a string of disconnected experiments and starts being a measured loop with a version history you can defend.</p>",
  "author": {
    "name": "Skill Index Editorial"
  },
  "publishedAt": "2026-06-03",
  "updatedAt": "2026-06-03",
  "image": {
    "url": "/images/blog/best-claude-code-skills-for-prompt-engineers.webp",
    "alt": "Prompt engineer's pastel desk flat-lay with an open laptop showing a prompt-evaluation dashboard with a side-by-side Claude/GPT/Gemini model comparison table of latency, tokens, and cost plus a green 'LLM judge 8.4/10' score gauge, a second monitor showing a prompt-version history diff with green added lines and a structured-output JSON schema panel, pastel sticky notes labeled 'eval', 'judge', 'v3 prompt', and 'context', a small index-card stack labeled 'prompt library', a coffee mug, and a printed prompt-iteration loop diagram, in a soft pastel palette",
    "width": 1200,
    "height": 675
  },
  "category": "Guides",
  "tags": [
    "prompt-engineers",
    "prompt-engineering",
    "prompt-evaluation",
    "llm-as-judge",
    "eval-harness",
    "structured-output",
    "prompt-versioning",
    "context-management",
    "rag",
    "prompt-library",
    "listicle",
    "skills",
    "claude-code"
  ],
  "readingTime": 7,
  "featured": true,
  "faq": [
    {
      "question": "Which Claude Code skill should a prompt engineer install first?",
      "answer": "Install gstack first and use its benchmark-models skill as the gate for every prompt-or-model choice. The gstack plugin has 104,138 stars and an MIT license and is the largest, most active plugin in the index. benchmark-models runs the same prompt through Claude, GPT (via the Codex CLI), and Gemini side-by-side and compares latency, tokens, cost, and optionally quality via an LLM judge. For a prompt engineer this is the single most useful thing in the index because it answers the question that otherwise gets answered by guesswork — which model is actually best for this prompt, with data instead of vibes. The LLM-judge option is the part that matters most: it turns 'this response feels better' into a scored, repeatable comparison you can run again after changing one line of the prompt. It is deliberately separate from gstack's page-performance benchmark skill so the names do not collide."
    },
    {
      "question": "What is the best Claude Code skill for structured output and JSON prompts?",
      "answer": "Use firecrawl-agent from the cli plugin (425 stars, verified, by Firecrawl). It is an autonomous extraction agent that navigates complex multi-page sites and returns structured JSON against a schema you supply. The prompt-engineering value is structured output the honest way: instead of begging a prompt to 'respond only in JSON' and hoping it holds, you hand the tool an explicit JSON schema and get conformant data back. That habit — constrain the shape, then validate against it — is the same discipline you want in every production prompt that feeds a downstream system, and seeing it enforced end to end is a useful reference for designing your own structured-output prompts. It is also a fast way to build the input side of an eval set: point it at real pages and get clean, schema-shaped records to test prompts against."
    },
    {
      "question": "How do prompt engineers version and reuse a prompt that finally works?",
      "answer": "Use skillify from gstack. It walks back through the conversation, takes the most recent successful flow, and codifies it into a permanent, tested skill on disk — script, test, and fixture — so the next run executes the saved version instead of re-deriving it. For a prompt engineer this is prompt versioning with teeth: the prompt or flow that finally worked stops living in a scrollback you will lose and becomes a named, committed, test-covered artifact. The generated test is the quiet payoff — it pins the behavior, so when you tweak the prompt later you find out immediately whether you improved it or broke it, which is the regression check most prompt work never has. Skillify the moment a prompt crosses from 'experimenting' to 'I want this exact thing again,' and pair it with benchmark-models so the version you codify is the one that actually won the comparison."
    },
    {
      "question": "How do I stop rediscovering the same prompt fix over and over?",
      "answer": "Use learn from gstack — a persistent store of what the project has learned across sessions, with commands to review, search, prune, and export it, plus a proactive nudge when you ask about a past pattern or wonder 'didn't we solve this before?' For a prompt engineer this is the pattern library that actually gets used, because it is searched from inside the same tool doing the work rather than living in a doc nobody opens. The recurring waste in prompt work is rediscovering the same fix — the phrasing that stops a model from hedging, the system-prompt clause that fixes a refusal, the few-shot ordering that lifts accuracy — and a searchable learnings store is the cheapest defense against re-solving it every time. Write a learning whenever a change produces a non-obvious improvement, search it before you reinvent a technique, and prune on a cadence so stale tricks do not mislead."
    },
    {
      "question": "How do I keep context across a multi-day prompt iteration?",
      "answer": "Use the context-save and context-restore pair from gstack. context-save captures git state, the decisions made so far, and the remaining work into a durable artifact; context-restore loads the most recent saved state — across branches and even across workspace handoffs — so you resume exactly where you left off. Prompt iteration is long: by version ten you are holding a dozen tradeoffs in your head — why you dropped the chain-of-thought block, which few-shot example caused the regression, what the judge flagged last run — and a context window does not survive a session boundary. Save at the end of any session you will not finish in one sitting, before you log off rather than after you have lost the thread, and restore at the start of the next session before you touch the prompt so you are editing the version that was actually winning."
    },
    {
      "question": "Where should a prompt engineer keep a reusable prompt library?",
      "answer": "Use obsidian-cli from the obsidian-skills plugin (33,445 stars, MIT, verified). It reads, creates, searches, and manages notes in an Obsidian vault straight from the command line, which makes it the durable home for a prompt library — reusable system prompts, few-shot sets, eval rubrics, and the reasoning behind each version, living as real, linkable, searchable documents instead of scattered text files. The advantage over gstack's in-session learn store is permanence and structure: wikilinks let you connect a prompt to its eval results and its judge rubric, so the library becomes a graph you can navigate rather than a flat log. Build prompts in the tool that runs them, file the keepers in the vault, and keep learn and the vault in sync so fast in-session capture and durable storage reinforce each other."
    },
    {
      "question": "How do I prepare clean grounding context for a RAG or retrieval prompt?",
      "answer": "Use firecrawl-parse from the cli plugin (425 stars, verified, by Firecrawl). It turns a local document — PDF, DOCX, XLSX, HTML, and more — into clean markdown on disk, with options for an AI summary or a direct question against the file. The prompt-engineering use is grounding-context preparation: the quality of a RAG or retrieval prompt is capped by the quality of the text you feed it, and a PDF dumped raw into a prompt is full of headers, footers, and layout noise that wastes tokens and confuses the model. firecrawl-parse produces the clean, structured markdown you actually want as context, saved to disk so you read it incrementally with grep and head rather than blowing up the context window. Parse first, inspect the markdown, then feed only the relevant sections — better grounding data is the highest-leverage, least-glamorous fix in most retrieval prompts."
    }
  ]
}
